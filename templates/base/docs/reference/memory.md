# Memory

## Durable Truths

- Root discovery index must list every must-read local guide.
- `templates/base/AGENTS.md` remains the constitutional root and first-read index for the copied hierarchy.
- Shipped and optional skill-package truth stay separate.

## Decisions to Preserve

- The approved local guides are `docs/AGENTS.md`, `docs/live/AGENTS.md`, `docs/reference/AGENTS.md`, `.agents/AGENTS.md`, `.agents/skills/AGENTS.md`, and `.agents/skills-optional/AGENTS.md`.
- Template live docs remain inert until a copied repo localizes them.
- Optional packages are not shipped default truth and must not be claimed unless they are present and enabled.
