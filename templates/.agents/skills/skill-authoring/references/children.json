{
  "router_name": "skill-authoring",
  "purpose": "Route skill-package authoring requests to leaf skill creation or router-family creation.",
  "selection_order": [
    "Leaf skill or single-job package -> create-skill",
    "Router family with nested children and install-or-fallback behavior -> create-router-skill"
  ],
  "children": [
    {
      "name": "create-skill",
      "target": "skill-authoring/create-skill",
      "relationship": "routes_to",
      "summary": "Creates or upgrades reusable leaf skill packages.",
      "route_when": [
        "The package is a single repeatable job rather than a family router.",
        "The work needs portable leaf guidance, references, scripts, assets, or evals but not child selection."
      ],
      "avoid_when": [
        "The package's main job is routing among children.",
        "The work needs explicit child metadata, install hints, or fallback behavior."
      ],
      "requires": [],
      "recommends": [],
      "fallbacks_to": [],
      "install_if_missing": {
        "package": "skill-authoring/create-skill",
        "notes": "Install this nested child path before routing if the runtime cannot discover bundled children automatically."
      }
    },
    {
      "name": "create-router-skill",
      "target": "skill-authoring/create-router-skill",
      "relationship": "routes_to",
      "summary": "Creates or upgrades discoverable router skills with explicit child inventories.",
      "route_when": [
        "The package's main job is choosing among child skills.",
        "The family boundary, child order, or install-or-fallback behavior must be explicit."
      ],
      "avoid_when": [
        "The work is a leaf skill with one repeatable job.",
        "The package would only be a category label or umbrella without decision logic."
      ],
      "requires": [],
      "recommends": [],
      "fallbacks_to": [],
      "install_if_missing": {
        "package": "skill-authoring/create-router-skill",
        "notes": "Install this nested child path before routing if the runtime cannot discover bundled children automatically."
      }
    },
    {
      "name": "create-agents",
      "target": "skill-authoring/create-agents",
      "relationship": "routes_to",
      "summary": "Creates reusable agent manifests or small agent teams with explicit model profile metadata and harness-style contracts.",
      "route_when": [
        "The package is a reusable agent manifest, not a skill package.",
        "The work needs ready-to-use task, explore, plan, librarian, or similar specialist agents."
      ],
      "avoid_when": [
        "The job is clearly a leaf skill or router family under the skill package system.",
        "The request is only a one-off chat instruction that should stay outside the repo."
      ],
      "requires": [],
      "recommends": [],
      "fallbacks_to": [],
      "install_if_missing": {
        "package": "skill-authoring/create-agents",
        "notes": "Install this nested child path before routing if the runtime cannot discover bundled children automatically."
      }
    }
  ]
}
